[tool.poetry]
name = "cost"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
eth-ape = "^0.7.5"
ape-vyper = "^0.7.0"
ape-arbitrum = "^0.7.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
